{
  "private": true,
  "name": "evergarden",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "yarn workspaces run build"，
    "storybook": "yarn workspace evergarden run storybook"
  }
}
